// Next.js extends the React preset. eslint-config-next is installed at the
// app level (apps/marketing) and composed there to keep its native plugins
// scoped to where Next runs.
import reactConfig from "./react.mjs";

export default reactConfig;
